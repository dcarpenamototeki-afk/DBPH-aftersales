import { AppShell } from "@/components/shell";
import { UnidentifiedPlatesPage } from "@/components/unidentified-plates-page";

export default function Page() {
  return (
    <AppShell>
      <UnidentifiedPlatesPage />
    </AppShell>
  );
}
